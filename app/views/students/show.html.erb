<p style="color: green"><%= notice %></p>

<%= render @student %>

<div>
  <%= link_to "Edit this student", edit_student_path(@student), class: "btn btn-sm btn-primary" %> |
  <%= link_to "Back to students", students_path, class: "btn btn-sm btn-primary" %>

  <%= button_to "Destroy this student", @student, method: :delete %>
</div>
