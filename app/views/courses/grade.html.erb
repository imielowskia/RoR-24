
<div class="container bg-secondary bg-gradient bg-opacity-10">
  <div class="row">
    <div class="col-4"><h2><%= @course.nazwa %></h2></div>
    <div class="col-2"><%= link_to "Edit", edit_course_path(@course), class: "btn btn-sm btn-primary"%></div>
    <div class="col-2"><%= link_to "Back to courses", courses_path, class: "btn btn-sm btn-primary" %></div>
  </div>
  <div class="row">
    <div class="col-4"><h3>Oceny</h3></div>
    <div class="col-2"></div>
    <div class="col-2"><%= link_to 'Oceń', grade_group_path(@course.id, @group.id), class: "btn btn-sm btn-primary" %> </div>
  </div>
  <% @students.each do |s| %>
    <div class="row border-bottom">
      <div class="col">
        <%= @grades[s.id]['imie'] %> <%= @grades[s.id]['nazwisko'] %>
      </div>
      <div class="col-2"><%= @grades[s.id]['grade'] %> </div>

    </div>
  <% end %>
</div>